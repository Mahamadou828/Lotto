import { createSlice } from "@reduxjs/toolkit";
import { ISearch } from "../typescript";

const searchSlice = createSlice({
  name: "searchSlice",
  initialState: {} as ISearch,
  reducers: {},
});
